#include <stdio.h>

int main(void) {
  double tab[10]; //tablica//
  double start; //pierwszy wyraz ciagu//
  double stop; //ostatni wyraz ciagu//
  double r; //roznica ciagu// 
  //print("Podaj wartosc pierwszego wyrazu ciagu arytmetycznego: ");//
  //scanf("%lf", &start);//
  //printf("Podaj wartosc ostaniego wyrazu ciagu arytmetycznego: ");//
  //scanf("%lf", &stop);//
  scanf("%lf %lf", &start, &stop);
  r=(stop-start)/9; //roznica ciagu arytmetycznego r=(an-a1)/(n-1)//
  int i;
  for( i=0; i<10 ; i++ ){
      tab[i]=stop-i*r; //kolejne wyrazy ciagu arytmetycznego//
      printf("%.3lf ", tab[i]);
  }
    
    return 0;
}